# 数据对比分析脚本使用说明

## 脚本功能
这个脚本用于对比线上和线下的数据异常情况，找出：
- 两边都异常的数据
- 仅线上异常的数据  
- 仅线下异常的数据

## 主要改进
相比原版代码，简化版本有以下改进：

### 1. 更清晰的代码结构
- 将复杂逻辑分解为8个简单步骤
- 每个步骤都有明确的注释说明
- 使用`main()`函数组织代码，更易理解

### 2. 简化的列查找
- 原版：复杂的正则表达式和模糊匹配
- 简化版：简单的字符串包含匹配，更直观

### 3. 更好的错误处理
- 逐步检查每个操作的结果
- 清晰的错误提示信息
- 避免程序突然崩溃

### 4. 更详细的输出信息
- 每个步骤都有进度提示
- 最终结果有清晰的统计信息
- 便于调试和验证

## 使用方法

### 1. 修改文件路径
在脚本开头修改这两个路径：
```python
online_file = "你的线上数据文件路径.csv"
raw_file = "你的线下数据文件路径.csv"
```

### 2. 运行脚本
```bash
python simplified_data_analysis.py
```

### 3. 查看结果
脚本会在线下数据文件的同一目录下生成 `核对线上线下查无样本.csv` 文件。

## 输出文件说明
结果文件包含以下列：
- `身份证`：身份证号码
- `手机号`：手机号码
- `线上状态`：线上数据的状态信息
- `线下状态`：线下数据的状态信息
- `对比结果`：标记数据来源（两边都异常/仅线上异常/仅线下异常）

## 注意事项
1. 确保CSV文件路径正确
2. 确保数据文件包含身份证和手机号列
3. 如果遇到编码问题，脚本会自动尝试多种编码格式
4. 建议先在小数据集上测试

## 常见问题
**Q: 提示"未找到身份证列"怎么办？**
A: 检查你的CSV文件列名，确保包含"id"、"身份证"、"身份证号"、"idcard"、"证件号"等关键词之一。

**Q: 提示"未找到手机号列"怎么办？**
A: 检查你的CSV文件列名，确保包含"phone"、"mobile"、"手机号"、"手机"等关键词之一。

**Q: 结果文件是空的怎么办？**
A: 检查筛选条件，确保数据中确实存在符合条件的记录。